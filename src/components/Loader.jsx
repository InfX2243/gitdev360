import React from "react";

const Loader = () => {
    return (
        <div className="loader">
            <div className="spinner">
                <p>Loading...</p>
            </div>
        </div>
    );
};

export default Loader;

