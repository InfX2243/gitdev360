const HeroSection = () => {
    return (
        <section className="hero">
            <h2>A 360° view of any GitHub developer</h2>
            <p>
                Analyze repositories, activity, languages, and stats - all in one place.
            </p>
        </section>
    );
};

export default HeroSection;